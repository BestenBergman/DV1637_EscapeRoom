using NUnit.Framework;
using UnityEngine;

public class H_PlayerStats : MonoBehaviour
{
    public bool hasItem = false;

    public Vector3 HoldPos = new Vector3(0, 0, 2);

    public string[] itemTags = { "item", "R1_Green", "R1_Red", "R1_Blue" };
}
